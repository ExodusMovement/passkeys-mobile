pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

include ':network.passkeys.client'
project(':network.passkeys.client').projectDir = new File('../../android')

rootProject.name = "PasskeysAndroid"
include ':app'
